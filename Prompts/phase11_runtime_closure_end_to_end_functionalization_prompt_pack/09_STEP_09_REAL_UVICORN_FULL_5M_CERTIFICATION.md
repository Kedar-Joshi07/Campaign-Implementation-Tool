# Step 9 — Real uvicorn Full 5M Certification

This is the decisive runtime proof.

Preconditions:
- implementation committed;
- clean git status;
- canonical 5M demographics validated;
- exact baseline/head recorded;
- no test-server executor injection.

Launch:
`python -m uvicorn app.main:app`

From installed browser:
Home → Find Potential Customers → submit a valid request requiring a true current Phase10 scoring path.

The browser submission must trigger the real app coordinator.

Verify when required:
- exact Phase10 compatibility decision;
- analysis/model reuse or build;
- scoring full 5,000,000;
- distinct = 5,000,000;
- duplicates = 0;
- invalid/nonfinite/out-of-range = 0;
- 100 rank boundaries;
- current analytics;
- snapshot publication;
- completed search;
- Results display.

Then run:
1. exact repeat → same snapshot, zero membership-source scan.
2. targeting-filter change → same scoring/generation, new snapshot.
3. delivery-profile change → same membership/intelligence, export-only change.

Capture all analytical IDs, timings and checksums.

Do not corrupt/delete valid assets merely to force a new build.
If current source legitimately requires only scoring refresh, certify that exact correct path.

Create:
`docs/evidence/phase11_runtime_closure/09_REAL_APP_FULL_5M_CERTIFICATION.md`
plus machine-readable JSON.

STOP.
