# Step 15 — Clean-Head Full 5M Phase 10 Certification

This is the decisive production-scale test.

Preconditions:
- all Steps 1–14 implementation committed
- git status empty
- no dirty override
- exact HEAD recorded
- canonical LFS/source hashes verified
- runtime/evidence paths isolated according to repo policy

Select and record a deterministic valid Modeling Context that has enough history and
requires a genuinely new Phase10 generation for the intended full-build path.
Preflight historical counts are allowed; pretraining/prescoring to force success is not.

## Browser initiation mandatory
Installed Chrome/Edge business user:
Campaign Details → Campaign Context → Targeting Preferences → Target Group Preview.
This browser action must trigger automatic preparation. Do not initiate the decisive build only by script.

Certify as applicable:
1 exact context/hash
2 historical compatibility decision
3 Historical Analysis
4 eligibility P/U
5 governed PRIMARY model training
6 model validation
7 full current 5M prospect scoring
8 exact 100-boundary rank preparation
9 current analytics snapshot
10 final Phase9 source binding
11 exact Target Group Preview
12 Saved Target Group
13 Campaign Draft

If an exact compatible generation unexpectedly causes full reuse, choose another valid exact
context requiring the missing stages; do not delete/corrupt valid data to manufacture the test.

Full scoring assertions:
- demographic source = 5,000,000
- scored = 5,000,000
- distinct = 5,000,000
- duplicates = 0
- invalid/nonfinite/out-of-range = 0
- min/mean/max recorded
- deterministic sample rescore >=256 under existing tolerance contract
- keyset/chunk scoring only
- no OFFSET
- no whole-5M DataFrame/fetchall/list

Reuse follow-up:
create another campaign with same Modeling Context but different delivery channel/details;
prove no new training/scoring. Then change only prospect targeting filters and prove no rebuild.

Save at least one multi-branch Target Group and verify branch SHA, exact count,
deduplication, campaign linkage, legacy safe reopen behavior and pre-export PII boundary.

Run full regression:
pytest, Phase1–7 clean-room, Phase8 harness, Phase9 tests, Phase10 tests,
compileall, pip check, diff hygiene, repo/LFS hygiene, SQLite integrity_check.

Create machine-readable manifest plus:
`docs/evidence/phase10/15_FULL_5M_CERTIFICATION.md`

Record clean HEAD, browser/version, context/hash, orchestration/analysis/model/scoring IDs,
checksums, per-stage runtime, counts/metrics, reuse/build decisions, final group/campaign IDs.

STOP.
