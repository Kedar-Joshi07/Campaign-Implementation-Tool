# Step 6 — Browser: Model Training & Job Lifecycle

Select the fresh CURRENT historical analysis in UI.

Exercise every visible training configuration/control and start training by click.

Observe actual UI lifecycle QUEUED→RUNNING→COMPLETED, button/progress states and responsiveness.

Verify displayed source analysis, P/U, train/validation counts, exact feature count, candidate comparison, Bagging primary, Elkan challenger, Naive diagnostic, governed selection, artifact verification, quality flags and Top X% metric labels.

Use backend reads only to independently confirm exact 11 features, no prohibited features, contract/policy versions and artifact SHA.

Test form/duplicate-submission protections. STOP.
