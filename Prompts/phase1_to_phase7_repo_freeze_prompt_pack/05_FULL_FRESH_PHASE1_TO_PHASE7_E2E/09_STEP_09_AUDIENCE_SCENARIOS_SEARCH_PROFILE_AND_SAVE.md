# Step 9 — Audience Scenarios, Search, Profile & Save

Through UI execute:
1. no filters ALL_MATCHING → 5M
2. top 1% → 50,000
3. top decile → 500,000
4. real demographic filter
5. rank + demographic filter
6. TOP_N ~50,000 if matching population supports it

Search assertions: allowed fields only, no contact PII/sensitive fields, no duplicate person IDs across pages.

Profile: universe/matching/selected/historical-positive aggregate populations reconcile and disclaimers remain visible.

Save an export-ready audience via UI, reopen it and verify exact filters/selection/resolved count/currentness. Backend may independently verify immutable hash/provenance. STOP.
