# Step 3 — Clean-Room Phases 2→5

Through public service/API layer create a real historical analysis; verify P+U=selected; persist/reopen it; train real PU model; exercise QUEUED→RUNNING→COMPLETED; verify Bagging primary, Elkan challenger, Naive diagnostic, exact 11 features and artifact SHA.

Score the entire bounded prospect universe; verify one finite [0,1] score per person, provenance and deterministic rescore sample. Exercise stale-job recovery in isolated DB. STOP.
