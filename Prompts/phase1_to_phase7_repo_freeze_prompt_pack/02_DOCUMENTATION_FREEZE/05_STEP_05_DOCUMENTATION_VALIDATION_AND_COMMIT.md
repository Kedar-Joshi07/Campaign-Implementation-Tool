# Step 5 — Documentation Validation & Commit

Verify every documented path, script, CLI, API, config variable, schema/contract version and page/control name against code.

Run pytest, compileall, pip check and git diff --check. Update tests only when they encode genuinely stale wording, never weaken functional assertions.

Create `docs/evidence/DOCUMENTATION_FREEZE_REPORT.md`.

Commit: `docs: complete phase1-7 master documentation freeze`

Record SHA. STOP.
